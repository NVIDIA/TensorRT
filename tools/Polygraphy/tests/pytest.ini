[pytest]
markers =
    serial: Indicates tests do not support parallel execution
    slow: Indicates tests that are slow and should be skipped if we choose not to run all tests
    multigpu: Indicates tests that require multiple GPUs (use min_gpus kwarg to specify the minimum count). For NCCL4Py + TensorRT, run via make test_multigpu
